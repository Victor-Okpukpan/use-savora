/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CYCLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  189, 110, 197, 59, 103, 0, 241, 115,
]);

export function getCycleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CYCLE_DISCRIMINATOR);
}

export type Cycle = {
  discriminator: ReadonlyUint8Array;
  bump: number;
  group: Address;
  /** Global cycle index; the `Cycle` PDA seed. */
  index: number;
  /** Which rotation this round belongs to (`= group.rotations_done` at open). */
  rotationIndex: number;
  /** Member slot that collects this cycle: `group.members[recipient_index]`. */
  recipientIndex: number;
  openedAt: bigint;
  /** `= opened_at + group.cycle_secs`. */
  deadline: bigint;
  /**
   * Base units gathered for this round: contributions plus any forfeited
   * deposits credited by the crank.
   */
  pooled: bigint;
  /**
   * Bitmask over member slots: bit `i` set = slot `i` has settled this
   * round. The recipient's bit is pre-set at open (they owe nothing).
   */
  contributed: number;
  /**
   * `= group.live_mask()` snapshotted at open — who is on the hook this
   * round. Frozen for the life of the cycle.
   */
  required: number;
  /** Slots ejected by this round's crank. Audit trail; `⊆ required`. */
  ejectedHere: number;
  disbursed: boolean;
  /** Amount actually paid to the recipient. */
  payout: bigint;
};

export type CycleArgs = {
  bump: number;
  group: Address;
  /** Global cycle index; the `Cycle` PDA seed. */
  index: number;
  /** Which rotation this round belongs to (`= group.rotations_done` at open). */
  rotationIndex: number;
  /** Member slot that collects this cycle: `group.members[recipient_index]`. */
  recipientIndex: number;
  openedAt: number | bigint;
  /** `= opened_at + group.cycle_secs`. */
  deadline: number | bigint;
  /**
   * Base units gathered for this round: contributions plus any forfeited
   * deposits credited by the crank.
   */
  pooled: number | bigint;
  /**
   * Bitmask over member slots: bit `i` set = slot `i` has settled this
   * round. The recipient's bit is pre-set at open (they owe nothing).
   */
  contributed: number;
  /**
   * `= group.live_mask()` snapshotted at open — who is on the hook this
   * round. Frozen for the life of the cycle.
   */
  required: number;
  /** Slots ejected by this round's crank. Audit trail; `⊆ required`. */
  ejectedHere: number;
  disbursed: boolean;
  /** Amount actually paid to the recipient. */
  payout: number | bigint;
};

/** Gets the encoder for {@link CycleArgs} account data. */
export function getCycleEncoder(): FixedSizeEncoder<CycleArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["group", getAddressEncoder()],
      ["index", getU16Encoder()],
      ["rotationIndex", getU8Encoder()],
      ["recipientIndex", getU8Encoder()],
      ["openedAt", getI64Encoder()],
      ["deadline", getI64Encoder()],
      ["pooled", getU64Encoder()],
      ["contributed", getU16Encoder()],
      ["required", getU16Encoder()],
      ["ejectedHere", getU16Encoder()],
      ["disbursed", getBooleanEncoder()],
      ["payout", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CYCLE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Cycle} account data. */
export function getCycleDecoder(): FixedSizeDecoder<Cycle> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["group", getAddressDecoder()],
    ["index", getU16Decoder()],
    ["rotationIndex", getU8Decoder()],
    ["recipientIndex", getU8Decoder()],
    ["openedAt", getI64Decoder()],
    ["deadline", getI64Decoder()],
    ["pooled", getU64Decoder()],
    ["contributed", getU16Decoder()],
    ["required", getU16Decoder()],
    ["ejectedHere", getU16Decoder()],
    ["disbursed", getBooleanDecoder()],
    ["payout", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Cycle} account data. */
export function getCycleCodec(): FixedSizeCodec<CycleArgs, Cycle> {
  return combineCodec(getCycleEncoder(), getCycleDecoder());
}

export function decodeCycle<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Cycle, TAddress>;
export function decodeCycle<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Cycle, TAddress>;
export function decodeCycle<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Cycle, TAddress> | MaybeAccount<Cycle, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCycleDecoder(),
  );
}

export async function fetchCycle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Cycle, TAddress>> {
  const maybeAccount = await fetchMaybeCycle(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCycle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Cycle, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCycle(maybeAccount);
}

export async function fetchAllCycle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Cycle>[]> {
  const maybeAccounts = await fetchAllMaybeCycle(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCycle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Cycle>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCycle(maybeAccount));
}

export function getCycleSize(): number {
  return 84;
}
