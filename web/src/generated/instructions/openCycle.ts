/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SAVORA_PROGRAM_ADDRESS } from "../programs";

export const OPEN_CYCLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  248, 147, 219, 61, 211, 57, 153, 30,
]);

export function getOpenCycleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(OPEN_CYCLE_DISCRIMINATOR);
}

export type OpenCycleInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountCycle extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountGroup extends string
        ? ReadonlyAccount<TAccountGroup>
        : TAccountGroup,
      TAccountCycle extends string
        ? WritableAccount<TAccountCycle>
        : TAccountCycle,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type OpenCycleInstructionData = { discriminator: ReadonlyUint8Array };

export type OpenCycleInstructionDataArgs = {};

export function getOpenCycleInstructionDataEncoder(): FixedSizeEncoder<OpenCycleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: OPEN_CYCLE_DISCRIMINATOR }),
  );
}

export function getOpenCycleInstructionDataDecoder(): FixedSizeDecoder<OpenCycleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getOpenCycleInstructionDataCodec(): FixedSizeCodec<
  OpenCycleInstructionDataArgs,
  OpenCycleInstructionData
> {
  return combineCodec(
    getOpenCycleInstructionDataEncoder(),
    getOpenCycleInstructionDataDecoder(),
  );
}

export type OpenCycleInput<
  TAccountPayer extends string = string,
  TAccountGroup extends string = string,
  TAccountCycle extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Permissionless: anyone can open the next cycle's account. The client
   * bundles this with the first `contribute` when the account is missing.
   */
  payer: TransactionSigner<TAccountPayer>;
  group: Address<TAccountGroup>;
  cycle: Address<TAccountCycle>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getOpenCycleInstruction<
  TAccountPayer extends string,
  TAccountGroup extends string,
  TAccountCycle extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SAVORA_PROGRAM_ADDRESS,
>(
  input: OpenCycleInput<
    TAccountPayer,
    TAccountGroup,
    TAccountCycle,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): OpenCycleInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountGroup,
  TAccountCycle,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SAVORA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    group: { value: input.group ?? null, isWritable: false },
    cycle: { value: input.cycle ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("group", accounts.group),
      getAccountMeta("cycle", accounts.cycle),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getOpenCycleInstructionDataEncoder().encode({}),
    programAddress,
  } as OpenCycleInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountGroup,
    TAccountCycle,
    TAccountSystemProgram
  >);
}

export type ParsedOpenCycleInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless: anyone can open the next cycle's account. The client
     * bundles this with the first `contribute` when the account is missing.
     */
    payer: TAccountMetas[0];
    group: TAccountMetas[1];
    cycle: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: OpenCycleInstructionData;
};

export function parseOpenCycleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOpenCycleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      group: getNextAccount(),
      cycle: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getOpenCycleInstructionDataDecoder().decode(instruction.data),
  };
}
