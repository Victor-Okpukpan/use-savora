/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SAVORA_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_EXTENSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([144, 243, 240, 58, 115, 44, 116, 187]);

export function getCancelExtensionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_EXTENSION_DISCRIMINATOR,
  );
}

export type CancelExtensionInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountGroup extends string
        ? WritableAccount<TAccountGroup>
        : TAccountGroup,
      ...TRemainingAccounts,
    ]
  >;

export type CancelExtensionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelExtensionInstructionDataArgs = {};

export function getCancelExtensionInstructionDataEncoder(): FixedSizeEncoder<CancelExtensionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_EXTENSION_DISCRIMINATOR }),
  );
}

export function getCancelExtensionInstructionDataDecoder(): FixedSizeDecoder<CancelExtensionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelExtensionInstructionDataCodec(): FixedSizeCodec<
  CancelExtensionInstructionDataArgs,
  CancelExtensionInstructionData
> {
  return combineCodec(
    getCancelExtensionInstructionDataEncoder(),
    getCancelExtensionInstructionDataDecoder(),
  );
}

export type CancelExtensionInput<
  TAccountSigner extends string = string,
  TAccountGroup extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  group: Address<TAccountGroup>;
};

export function getCancelExtensionInstruction<
  TAccountSigner extends string,
  TAccountGroup extends string,
  TProgramAddress extends Address = typeof SAVORA_PROGRAM_ADDRESS,
>(
  input: CancelExtensionInput<TAccountSigner, TAccountGroup>,
  config?: { programAddress?: TProgramAddress },
): CancelExtensionInstruction<TProgramAddress, TAccountSigner, TAccountGroup> {
  // Program address.
  const programAddress = config?.programAddress ?? SAVORA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    group: { value: input.group ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("group", accounts.group),
    ],
    data: getCancelExtensionInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelExtensionInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGroup
  >);
}

export type ParsedCancelExtensionInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    group: TAccountMetas[1];
  };
  data: CancelExtensionInstructionData;
};

export function parseCancelExtensionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelExtensionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), group: getNextAccount() },
    data: getCancelExtensionInstructionDataDecoder().decode(instruction.data),
  };
}
