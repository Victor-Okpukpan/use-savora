/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SAVORA_PROGRAM_ADDRESS } from "../programs";

export const OPT_IN_EXTENSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([238, 48, 27, 241, 149, 207, 27, 17]);

export function getOptInExtensionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPT_IN_EXTENSION_DISCRIMINATOR,
  );
}

export type OptInExtensionInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountMember extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMember extends string
        ? ReadonlySignerAccount<TAccountMember> &
            AccountSignerMeta<TAccountMember>
        : TAccountMember,
      TAccountGroup extends string
        ? WritableAccount<TAccountGroup>
        : TAccountGroup,
      ...TRemainingAccounts,
    ]
  >;

export type OptInExtensionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type OptInExtensionInstructionDataArgs = {};

export function getOptInExtensionInstructionDataEncoder(): FixedSizeEncoder<OptInExtensionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: OPT_IN_EXTENSION_DISCRIMINATOR }),
  );
}

export function getOptInExtensionInstructionDataDecoder(): FixedSizeDecoder<OptInExtensionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getOptInExtensionInstructionDataCodec(): FixedSizeCodec<
  OptInExtensionInstructionDataArgs,
  OptInExtensionInstructionData
> {
  return combineCodec(
    getOptInExtensionInstructionDataEncoder(),
    getOptInExtensionInstructionDataDecoder(),
  );
}

export type OptInExtensionInput<
  TAccountMember extends string = string,
  TAccountGroup extends string = string,
> = {
  member: TransactionSigner<TAccountMember>;
  group: Address<TAccountGroup>;
};

export function getOptInExtensionInstruction<
  TAccountMember extends string,
  TAccountGroup extends string,
  TProgramAddress extends Address = typeof SAVORA_PROGRAM_ADDRESS,
>(
  input: OptInExtensionInput<TAccountMember, TAccountGroup>,
  config?: { programAddress?: TProgramAddress },
): OptInExtensionInstruction<TProgramAddress, TAccountMember, TAccountGroup> {
  // Program address.
  const programAddress = config?.programAddress ?? SAVORA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    member: { value: input.member ?? null, isWritable: false },
    group: { value: input.group ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("member", accounts.member),
      getAccountMeta("group", accounts.group),
    ],
    data: getOptInExtensionInstructionDataEncoder().encode({}),
    programAddress,
  } as OptInExtensionInstruction<
    TProgramAddress,
    TAccountMember,
    TAccountGroup
  >);
}

export type ParsedOptInExtensionInstruction<
  TProgram extends string = typeof SAVORA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    member: TAccountMetas[0];
    group: TAccountMetas[1];
  };
  data: OptInExtensionInstructionData;
};

export function parseOptInExtensionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOptInExtensionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { member: getNextAccount(), group: getNextAccount() },
    data: getOptInExtensionInstructionDataDecoder().decode(instruction.data),
  };
}
